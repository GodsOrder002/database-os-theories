好，你这条纠正是关键：**`card_2` 里所有链条本质都是“我对组合态的推测/建模”**，`collect_fakes` 只是“权重大/优先参考”的证据入口，**不是推断合法性的来源**。

我把边界句按你的意思改掉（只改规则，不改你已有赞同的那几条）。

---

## 改正后的边界

### `collect_fakes`

**定位不变，但降级为“强参考”而非“必需来源”**：

> **高权重参考证据**：我收集到的出牌/信号集合；默认不可信；作用是提高/降低推断与裁决的权重，不构成推断的必要前提；不得被下游结果回写改造。

### `hunts_chain`

你说得对：它不必来自 `collect_fakes`，它是“推测依据牌组合”（可含先验/概念牌/规则牌/经验牌）：

> **纯推断链-依据**：用于推测“目标组合”的依据牌组合（可包含先验/概念/规则/经验，不要求来自 collect_fakes）；不得混入 my_hunts_chain；不包含混合裁决与最终行动结果。

（你赞同的“不混入 my_hunts_chain”我保留。）

### `chain_results`

你赞同的“不得反向改写”保留，同时把“来源限定”拿掉：

> **纯推断链-结果**：基于 hunts_chain（并可参考 collect_fakes 的权重）得到的目标组合推测结果；模型态（可多版本/可撤回）；不得反向改写 collect_fakes / hunts_chain。

### `my_hunts_chain`

同理：也是推测链，只是对象换成“我自己/我方目标”：

> **我方链-依据**：用于推测/构造我方猎杀（行动）组合的依据牌组合（可包含先验/概念/规则/经验，不要求来自 collect_fakes；可参考 collect_fakes 权重）；不包含混合裁决与最终结果。

### `mix_to_hunts_chain`

你原话定义非常准确：它就是把几条链放在一起做动态分析；这里我只加治理句：

> **混合编译体**：对 {my_hunts_chain + hunts_chain + chain_results + collect_fakes(权重参考)} 的动态整合；组合态产物；不得倒灌污染上游输入。

### `my_chain_results`

你赞同的“不得回写上游输入”保留：

> **我方链-结果**：仅由 mix_to_hunts_chain 生成的最终行动/猎杀组合；不得回写改造上游输入。

---

## 你这条纠正带来的一个“隐含要求”

既然 `hunts_chain / my_hunts_chain` 不必来自 `collect_fakes`，那治理上就需要一个硬边界来避免“凭空胡写”污染审计价值：

> **推测允许自由来源，但必须可区分“先验依据”与“证据参考”。**
> 否则以后复盘你会不知道：这次推断错，是因为先验错，还是因为证据权重误读。

我不说你该怎么存，只说边界：**必须能区分**。

---

你已经确认的三条边界我也记住了（不会再改口）：

* `hunts_chain` 不混入 `my_hunts_chain`
* `chain_results` 不得反向改写 `collect_fakes / hunts_chain`
* `my_chain_results` 不得回写上游输入

如果你愿意，我下一步就把这些边界句再压缩成“每列一句、极短版”（适合直接塞进 comment，不容易被自己未来误读）。
